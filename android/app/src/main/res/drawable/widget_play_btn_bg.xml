<?xml version="1.0" encoding="utf-8"?>
<!-- A true pill at whatever size it is used. The radius is deliberately far
     larger than any button here: GradientDrawable clamps it to half the
     shorter side, so one drawable gives fully round ends on both the small
     centre indicator and the larger panel button. The old fixed 20dp was less
     than half the 52dp panel button's height, which is what made it read as a
     squashed rectangle rather than a pill. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" />
    <corners android:radius="100dp" />
</shape>
